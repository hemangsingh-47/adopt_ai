// Placeholder for Passport configuration (e.g., Google OAuth in the future)
// As per standard JWT implementation, we use manual JWT generation and middleware.
// If passport-jwt or passport-google-oauth20 is added later, configure it here.

export const configurePassport = (passport) => {
  // Strategy configurations go here
};
